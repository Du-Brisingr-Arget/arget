for f in `cat repository.argt`; do 
    mkdir ./arget-tmp
    cd arget-tmp
    `git clone $f`
    gitname=$( ls )
    cd $gitname
    makepkg -sric --noconfirm
    cd ..
    rm -rf $gitname
    cd ..
done 
