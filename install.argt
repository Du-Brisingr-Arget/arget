for f in `cat repository.argt`; do 
    mkdir ./arget-tmp
    cd arget-tmp
    `git clone $f`
    gitname=$( ls )
    cd $gitname
    makepkg -sirc --noconfirm
    cd ..
    cd ..
    rm -rf arget-tmp
done 
